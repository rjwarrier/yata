<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The window background matters more than it looks. Between the process starting and Compose
        drawing its first frame, this is the only thing on screen — and the platform's
        Material.Light default is white, which is what produced the white flash on launch,
        including for anyone using the dark or AMOLED theme. Pointing it at a colour that has a
        -night variant means the gap is filled with something close to the app instead.
    -->
    <style name="Theme.Yata" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
    </style>

    <!--
        The launch theme, applied to MainActivity in the manifest and swapped for Theme.Yata by
        installSplashScreen() as soon as the Activity starts. postSplashScreenTheme is what
        performs that swap; without it the app would keep the splash theme for its whole life.
    -->
    <style name="Theme.Yata.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Yata</item>
    </style>

    <!-- Transparent full-size window — the Quick Add dialog activity draws its own centered
         card + scrim in Compose, so the underlying Android window itself must be invisible. -->
    <style name="Theme.Yata.Transparent" parent="Theme.Yata">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
