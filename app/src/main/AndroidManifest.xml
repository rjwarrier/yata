<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Package-visibility (API 30+) so RecognizerIntent.resolveActivity() can find the
         system speech-to-text UI for voice quick-add. -->
    <queries>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <application
        android:name=".YataApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yata"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <!-- WorkManager auto-init disabled — YataApplication provides its own Configuration
             (via Configuration.Provider) so HiltWorkerFactory can inject app workers. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Launches under the splash theme; installSplashScreen() swaps it for Theme.Yata via
             postSplashScreenTheme once the Activity is up. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Yata.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep links: yata://task/<id> and friends, so a task can be linked from a note,
                 an email, or an automation. Custom scheme rather than an https App Link, which
                 would need a verified assetlinks.json on a domain this app doesn't have. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yata" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Task reminder alarm receiver -->
        <receiver
            android:name=".notification.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mj.yata.REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Notification action receiver (snooze / complete from notification) -->
        <receiver
            android:name=".notification.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mj.yata.ACTION_COMPLETE_TASK" />
                <action android:name="com.mj.yata.ACTION_SNOOZE_TASK" />
                <action android:name="com.mj.yata.ACTION_SNOOZE_15M" />
                <action android:name="com.mj.yata.ACTION_SNOOZE_TOMORROW" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver - re-schedules alarms after reboot -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widget showing today's open tasks -->
        <receiver
            android:name=".widget.YataAppWidgetReceiver"
            android:label="@string/widget_today_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>

        <!-- Home-screen widget: quick add -->
        <receiver
            android:name=".widget.QuickAddWidgetReceiver"
            android:label="@string/widget_quick_add_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_add_widget_info" />
        </receiver>
        <activity
            android:name=".widget.WidgetCustomizerConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <activity
            android:name=".widget.QuickAddDialogActivity"
            android:exported="true"
            android:label="@string/share_to_task_label"
            android:theme="@style/Theme.Yata.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:windowSoftInputMode="adjustResize">
            <!-- Share-to-task: "Add to Yata" in other apps' share sheets, and on long-pressed
                 text selections anywhere on the device. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Quick Settings tile: quick-add task from the notification shade -->
        <service
            android:name=".widget.QuickAddTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_quick_add"
            android:label="@string/qs_tile_quick_add_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Home-screen widget: progress / stats -->
        <receiver
            android:name=".widget.ProgressStatsWidgetReceiver"
            android:label="@string/widget_progress_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/progress_widget_info" />
        </receiver>

        <!-- Home-screen widget: per-person overdue counts -->
        <receiver
            android:name=".widget.TeamOverdueWidgetReceiver"
            android:label="@string/widget_team_overdue_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/team_overdue_widget_info" />
        </receiver>

        <!-- Home-screen widget: single pinned list (user picks which at add-time) -->
        <receiver
            android:name=".widget.SingleListWidgetReceiver"
            android:label="@string/widget_single_list_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/single_list_widget_info" />
        </receiver>


        <!-- Home-screen widget: upcoming / agenda -->
        <receiver
            android:name=".widget.UpcomingWidgetReceiver"
            android:label="@string/widget_upcoming_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/upcoming_widget_info" />
        </receiver>

        <!-- Serves exported PDF/PNG entity reports (cache/exports/) to the share sheet -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Tasker plugin: Create Task action -->
        <activity
            android:name=".tasker.createtask.CreateTaskConfigActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Create Task">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity>

    </application>

</manifest>
