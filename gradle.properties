android.useAndroidX=true
# Jetifier stays off: nothing in the dependency graph pulls com.android.support, so enabling it
# only costs a rewrite pass over every dependency JAR on each build.
android.enableJetifier=false
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# Skips the configuration phase on repeat builds — the expensive part here, given Hilt + KSP.
org.gradle.configuration-cache=true
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=true
android.builtInKotlin=false
android.newDsl=false
